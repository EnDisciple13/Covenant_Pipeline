# Covenant mutation drill baseline (2026-07-05)

Executor: Composer 2.5 (agent-driven v0) | Stage 0 anchor: `tests/invariants/` (chunker-coverage, provenance-grounding, container-parity host reproducibility)

## Preconditions

- Clean baseline: `py -m pytest tests/invariants/ -q` → 6 passed, 1 skipped (docker parity)
- Golden fixture: `tests/fixtures/synthetic_covenant.pdf`
- Mutations applied one at a time; code reverted after each drill

## Kill matrix

| Mutant | Applied cleanly? | Invariants red | Killed? | Surviving-mutant work item |
|--------|------------------|----------------|---------|----------------------------|
| M1 Drop TOC section | Y | `chunker-coverage-audit` | Y | — |
| M2 Reorder chunks | Y (manual analysis) | — | N | Implement `chunker-partition` monotonicity test |
| M3 Fabricated citation span | Y (fixture surrogate) | `provenance-grounding` | Y | — |
| M4 Numeric → string | N/A (no compile-stage test in Stage 0) | — | N | Wire audit type-safety predicate test |
| M5 Circular glossary | N/A | — | N | Implement `glossary-acyclic` DFS test |
| M6 Silent env default | N/A | — | N | Implement `config-totality` test |
| M7 Bbox outside page | N/A | — | N | Extend `chunker-partition` bbox-in-page check |
| M8 Wrong receipt section | Y (same class as M3) | `provenance-grounding` | Y | — |

## Kill rate per invariant

| Invariant | Kills / targeted | Notes |
|-----------|------------------|-------|
| `chunker-coverage-audit` | 1/1 | M1 verified via `run_chunker` skeleton drop |
| `provenance-grounding` | 2/2 | M3/M8 class; fixture + helper verified |
| `container-parity` | 0/0 | No mutation in v0 list targets parity directly |
| *(missing)* `chunker-partition` | 0/2 | M2, M7 survived — expected suite gap |
| *(missing)* `config-totality` | 0/1 | M6 survived |
| *(missing)* `glossary-acyclic` | 0/1 | M5 survived |

## Narrative

**M1** — Patched `run_chunker` to drop the first TOC skeleton row before boundary calculation. `test_chunker_coverage_audit_non_empty_extraction` failed (silent omission). Reverted; baseline green again.

**M2, M6, M7** — No Stage 0 test enforces partition monotonicity, config totality, or bbox bounds. Survivors are work items per PE_Invariant_Suite enforcement gap (documented expected result in runbook).

**M3/M8** — Provenance helper rejects `audited_bad_span.json` (fabricated leaf not in rehydrated chunk text). Full-pipeline M8 drill deferred until LLM golden path is committed.

## Regression

Re-run after new invariant tests, mutation list changes, or model swap. Record in `tests/mutation/reports/` and update [README](../README.md) reports table.
